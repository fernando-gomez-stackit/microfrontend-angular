import { Route } from '@angular/router';
import { LoginRemoteEntryComponent } from './login-entry.component';

export const remoteRoutes: Route[] = [
  { path: '', component: LoginRemoteEntryComponent },
];
